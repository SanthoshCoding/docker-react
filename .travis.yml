sudo: required
services:
  - docker

before_install:
  - docker build -t dockertuka/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true dockertuka/docker-react npm run test -- --coverage
